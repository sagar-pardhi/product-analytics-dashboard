PORT=4000
FRONTEND_URL=http://localhost:5173

# --- PRODUCTION (Neon DB) ---
# If set, Neon DB is used automatically. Get from Neon dashboard → Connection string.
# DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# --- LOCAL DEVELOPMENT (standard postgres) ---
# Used only when DATABASE_URL is NOT set.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=product_analytics
DB_USER=postgres
DB_PASSWORD=postgres
